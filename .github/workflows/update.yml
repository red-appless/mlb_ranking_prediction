# name: MLB Update

# on:
#   schedule:
#     - cron: '0 21 * * *' # 日本時間 朝6時
#   workflow_dispatch:
#   repository_dispatch:
#     types: [manual-update]

# jobs:
#   update:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: write
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-python@v5
#         with:
#           python-version: '3.10'
#       - run: pip install requests
#       - run: python update_standings.py
#       - name: Commit changes
#         run: |
#           git config --global user.name 'github-actions'
#           git config --global user.email 'github-actions@github.com'
#           git add data.json
#           git diff --quiet && git diff --staged --quiet || (git commit -m "Update MLB data" && git push)